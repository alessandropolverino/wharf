# `__init__.py`

The `wharf` package root. Holds a single value: `__version__`, read by
[`update_check.py`](update_check.md) to compare against the latest
published GitHub release tag. No other package-level state or imports.
